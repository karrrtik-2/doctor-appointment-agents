# The base URL of the FastAPI backend (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8003

# Default tenant for multi-tenant deployments
NEXT_PUBLIC_DEFAULT_TENANT_ID=default

# App environment: development | staging | production
NODE_ENV=development
